# Page content components

Next.js treats any `src/pages/*.{js,jsx}` file as a **Pages Router** route, which conflicts with the App Router in `src/app/`.

Page-level content components live in **`src/components/pages/`** instead. Route files remain in `src/app/`.
